services:
  - type: web
    name: veggies-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /api/*
        destination: https://YOUR_BACKEND_URL_HERE/api/*
      - type: rewrite
        source: /*
        destination: /index.html